import React, {useState} from "react";
import { Button, Form } from "react-bootstrap";
import { useForm } from "react-hook-form";

const ArrayRepresentation = () => {

    return (
        <div>
            <h1>
                Chapter 6.2 - Array Representation
            </h1>
        </div>
    );
};

export default ArrayRepresentation
