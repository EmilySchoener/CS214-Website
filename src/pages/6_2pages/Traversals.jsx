import React, {useState} from "react";
import { Button, Form } from "react-bootstrap";
import { useForm } from "react-hook-form";

const Traversals = () => {

    return (
        <div>
            <h1>
                Chapter 6.2 - Preorder, Inorder, and Postorder Traversals
            </h1>
        </div>
    );
};

export default Traversals
