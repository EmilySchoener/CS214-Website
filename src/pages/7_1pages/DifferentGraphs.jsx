import React, {useState} from "react";
import { Button, Form } from "react-bootstrap";
import { useForm } from "react-hook-form";

const DifferentGraphs = () => {

    return (
        <div>
            <h1>
                Chapter 7.1 - Different Graphs
            </h1>
        </div>
    );
};

export default DifferentGraphs