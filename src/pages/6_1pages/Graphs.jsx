import React, {useState} from "react";
import { Button, Form } from "react-bootstrap";
import { useForm } from "react-hook-form";

const Graphs = () => {

    return (
        <div>
            <h1>
                Chapter 6.1 - Lots of Graphs
            </h1>
        </div>
    );
};

export default Graphs